{% extends 'base.html.twig' %}

{% block title %}UserParticipation{% endblock %}

{% block body %}
    <h1>UserParticipation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_participation.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_participation_index') }}">back to list</a>

    <a href="{{ path('user_participation_edit', {'id': user_participation.id}) }}">edit</a>

    {{ include('user_participation/_delete_form.html.twig') }}
{% endblock %}
